Philosophers is a concurrent programming project based on the classic Dining Philosophers problem. It simulates multiple philosophers sitting around a table, alternating between thinking, eating, and sleeping while competing for shared resources (forks).

The project focuses on thread management, synchronization mechanisms, mutexes, timing control, and preventing race conditions, deadlocks, and resource starvation. Each philosopher's state is monitored in real time according to configurable simulation parameters.
